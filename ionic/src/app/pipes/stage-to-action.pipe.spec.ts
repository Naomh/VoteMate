import { StageToActionPipe } from './stage-to-action.pipe';

describe('StageToActionPipe', () => {
  it('create an instance', () => {
    const pipe = new StageToActionPipe();
    expect(pipe).toBeTruthy();
  });
});
