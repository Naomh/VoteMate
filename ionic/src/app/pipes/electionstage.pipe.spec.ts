import { ElectionstagePipe } from './electionstage.pipe';

describe('ElectionstagePipe', () => {
  it('create an instance', () => {
    const pipe = new ElectionstagePipe();
    expect(pipe).toBeTruthy();
  });
});
